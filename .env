# This creates the 'alembic' directory and the alembic.ini config file
alembic init alembic
# --- Celery Configuration (Phase 2, Step 6) ---
# Ensure you have Redis or RabbitMQ running locally
CELERY_BROKER_URL="redis://localhost:6379/0" 
CELERY_RESULT_BACKEND="redis://localhost:6379/1"
